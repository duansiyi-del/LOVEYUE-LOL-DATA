# 环境变量清单. 复制成 .env.local 再填真实值; 这个文件只有变量名, 可以提交.
# 线上的值在 Vercel -> Settings -> Environment Variables 里配, 改完要 Redeploy.

# ---- 数据库 ----
# Vercel 接好 Postgres 之后自动注入, 只有本地开发才需要手填.
# 本地跑 npm run dev:local 时指向自己机器上的库, 例如:
#   postgres://你的用户名@localhost:5432/loveyue_dev
POSTGRES_URL=

# ---- 本地开发专用, 不要配到 Vercel ----
# 由 npm run dev:local 自动设置, 把 @vercel/postgres 换成本地 pg 驱动
# (见 src/lib/localPostgres.ts). 写在这里没用.
# LOCAL_PG=1
